# Python
__pycache__/
*.pyc
*.pyo

# Lambda layer build artifacts
deploy/lambda-layer/*.zip

# Test result files (generated by oha/load tester) — keep specific lab outputs committable
results/*.json

# Do not commit endpoint URLs (expiry / account-specific); see STUDENT_GUIDE.md
loadtest/endpoints.sh

# Temporary files
/tmp/
*.tmp

# Docker
.docker/

# AWS credentials (never commit)
.aws/
*.pem

# Not part of the student template
CLAUDE.md
generate_figures.py
